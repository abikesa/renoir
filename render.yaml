services:
  - type: web
    name: renoir
    env: python
    plan: free
    branch: father-son-esprit
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    autoDeploy: true
